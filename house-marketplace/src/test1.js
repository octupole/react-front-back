const id='password'
const value='Bamby'
const prevState={}
const newState={...prevState,[id]:value}
const lala={[id]: {[id]: 'oppo'}}

const pState='kk'
const nState='ci'+pState
console.log(newState, lala)
